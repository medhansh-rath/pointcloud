A code implementation to create a point cloud from a RGB and depth image pair using CUDA.

To install the visualizer:
```
sudo apt install pcl-tools
```
To use the algorithm with normalization (flag -n) and visualization (flag -v) run:
```
./fast_cloud rgb.jpg depth.png -n -v
```
